---
project: [Project Name]
date: YYYY-MM-DD
author: [Your Name]
status: draft | in-progress | complete
---

# CLAUDE.md

> **Template file** — This is a skeleton. The `/scaffold` skill copies it into your project root and fills in the project-specific sections. Do not edit this file directly; edit the copy in your project.
>
> **What CLAUDE.md does**: Claude Code reads this file to understand your project's tech stack, conventions, commands, and constraints. It's like a cheat sheet that helps Claude work correctly in your codebase.

---

## Project Description

_Describe the project in 2-3 sentences. Be specific about what it does and who it is for._

- **Name:** [e.g., "Acme Dashboard"]
- **Purpose:** [e.g., "A real-time analytics dashboard that helps marketing teams track campaign performance across channels."]
- **Project type:** new product | new feature | redesign | optimization
- **Current state:** greenfield | early development | mature | maintenance
- **Key users:** [e.g., "Marketing managers at mid-size B2B companies"]
- **User complexity:** single user type | multiple personas shared flows | multiple personas separate flows
- **Stakeholder landscape:** solo | single stakeholder | multiple aligned | multiple competing

---

## Tech Stack

_List the primary technologies used in this project. Be specific about versions where it matters._

| Layer | Technology | Version | Notes |
|-------|-----------|---------|-------|
| Language | [e.g., TypeScript] | [e.g., 5.x] | |
| Framework | [e.g., Next.js] | [e.g., 14.x] | App Router |
| Styling | [e.g., Tailwind CSS] | [e.g., 3.x] | |
| Database | [e.g., PostgreSQL] | [e.g., 15] | Via Supabase |
| ORM | [e.g., Prisma] | [e.g., 5.x] | |
| Testing | [e.g., Vitest] | [e.g., 1.x] | |
| Package Manager | [e.g., pnpm] | [e.g., 8.x] | |
| Deployment | [e.g., Vercel] | | |

---

## Key Conventions

_Rules and patterns that Claude should follow when working in this codebase._

### Naming
- [e.g., "Use camelCase for variables and functions, PascalCase for components and types."]
- [e.g., "File names use kebab-case: `user-profile.tsx`, not `UserProfile.tsx`."]
- [e.g., "Test files are co-located: `user-profile.test.tsx` next to `user-profile.tsx`."]

### Code Style
- [e.g., "Always use explicit return types on functions."]
- [e.g., "Prefer named exports over default exports."]
- [e.g., "Use `const` by default. Only use `let` when reassignment is necessary."]
- [e.g., "Error handling: always catch errors at the boundary, never silently swallow them."]

### Component Patterns
- [e.g., "UI components go in `src/components/ui/`. Feature components go in `src/features/[feature]/components/`."]
- [e.g., "Use server components by default. Add 'use client' only when interactivity is needed."]
- [e.g., "Props interfaces are defined in the same file as the component, above the component."]

### Git
- [e.g., "Commit messages follow Conventional Commits: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`."]
- [e.g., "Branch naming: `feature/short-description`, `fix/short-description`."]
- [e.g., "Never commit directly to `main`. Always use a feature branch."]

---

## File Structure

_Describe the project directory layout so Claude understands where things live. Replace the example below with your actual project structure._

```
project-root/
  AGENTS.md              # Agent instructions (filled by scaffold)
  CLAUDE.md              # This file
  research/              # All research artifacts (filled by discovery skill)
  docs/
    prd/                 # Product requirements (definition phase)
    decisions/           # Decision records
  prototypes/            # Working prototypes (prototyping phase)
  iterations/            # Iteration briefs (iteration phase)
  src/                   # Source code (adapt to your stack)
    [your directories]   # Organize by your framework's conventions
  tests/                 # Test files (adapt to your stack)
  public/                # Static assets (if applicable)
```

_Tip: Replace the generic `src/` and `tests/` sections above with your actual directory layout. For example, a Next.js project might have `src/app/`, `src/components/`, `prisma/`, etc. A Python project might have `app/`, `tests/`, `requirements.txt`, etc._

---

## Common Commands

_Commands Claude should know how to run. Fill in the commands for your specific tech stack. Delete sections that don't apply._

```bash
# Install dependencies
[e.g., pnpm install / pip install -r requirements.txt / bundle install]

# Start development server
[e.g., pnpm dev / python manage.py runserver / rails server]

# Run tests
[e.g., pnpm test / pytest / rspec]

# Build for production
[e.g., pnpm build / docker build . / N/A]

# Run linter
[e.g., pnpm lint / ruff check . / rubocop]

# Deploy
[e.g., pnpm deploy / fly deploy / heroku push]

# Database (if applicable)
[e.g., pnpm prisma migrate dev / python manage.py migrate / rails db:migrate]
```

---

## Do Not List

_Things Claude should NEVER do in this project. Be explicit. Add your own project-specific rules below._

- **Do not** commit `.env` files, API keys, secrets, or credentials.
- **Do not** push directly to `main` or `production` branches.
- **Do not** install new dependencies without discussing the rationale first.
- **Do not** delete research artifacts without explicit confirmation.
- **Do not** create new top-level directories without discussing the reasoning.
- **Do not** [add your own project-specific rules — e.g., "Do not modify database schema without confirmation"].

---

## Environment Variables

_List the environment variables the project uses. Never include actual values here._

| Variable | Purpose | Where to Get It |
|----------|---------|-----------------|
| `DATABASE_URL` | [e.g., PostgreSQL connection string] | [e.g., Supabase dashboard] |
| `NEXT_PUBLIC_API_URL` | [e.g., API base URL] | [e.g., Set per environment] |
| `[OTHER_VAR]` | [Purpose] | [Source] |

> Actual values are in `.env.local` (not committed to git). See `.env.example` for the template.

---

## Known Gotchas

_Things that are easy to get wrong or that may surprise someone new to the project._

- [e.g., "The research/ folder is the single source of truth for all discovery artifacts. Do not create research files elsewhere."]
- [e.g., "Hot reloading does not work for changes to [specific config file]. Restart the dev server."]
- [e.g., "The API is owned by another team. Do not modify files in /api."]
